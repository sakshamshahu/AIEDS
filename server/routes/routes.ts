const express = require('express')

// Create express router
const router = express.Router()

// // GET /simplehello
// router.get('/simplehello', (req : Request, res : Response) => {
//   res.send('Hello AIDES!');
// })

// Export router
module.exports = router