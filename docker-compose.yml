version: "3.3"

services:
  aides-client:
    container_name: aides-client
    build: 
      context: .
      dockerfile: ./docker/dev/client.dockerfile
    restart: always
    ports:
      - 3000:3000
    volumes:
      - ./client:/client
    env_file:
      - .env